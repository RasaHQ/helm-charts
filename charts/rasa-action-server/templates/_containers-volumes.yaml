{{- define "rasa-action-server.containers.volumes" -}}
- name: config-dir
  emptyDir: {}
{{- end -}}


{{- define "rasa-action-server.containers.volumeMounts" -}}
# Mount the temporary directory for the Rasa global configuration
- name: "config-dir"
  mountPath: "/.config"
{{- end -}}
